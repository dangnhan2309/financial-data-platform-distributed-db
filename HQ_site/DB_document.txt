# Create pdb
CREATE PLUGGABLE DATABASE PDB_HQ_SITE 
ADMIN USER gc_admin IDENTIFIED BY "Nhan0944906711"
ROLES = (DBA)
FILE_NAME_CONVERT = ('C:\APP\LENOVO\PRODUCT\26AI\ORADATA\FREE\PDBSEED\', 'C:\APP\LENOVO\ORADATA\PDB_HQ_SITE\');


#create table space :
-- Đảm bảo đang đứng trong PDB của dự án
ALTER SESSION SET CONTAINER = PDB_HQ_SITE;

-- Tạo Tablespace Master (Chứa thông tin sản phẩm, khách hàng, đối tác)
CREATE TABLESPACE TS_MASTER DATAFILE SIZE 100M AUTOEXTEND ON NEXT 50M;

-- Tạo Tablespace Sales (Chứa đơn hàng, hợp đồng, vận chuyển)
CREATE TABLESPACE TS_SALES DATAFILE SIZE 100M AUTOEXTEND ON NEXT 50M;

-- Tạo Tablespace Finance (Chứa hóa đơn, thanh toán, ngân hàng)
CREATE TABLESPACE TS_FINANCE DATAFILE SIZE 100M AUTOEXTEND ON NEXT 50M;

# Grant privilege :
CREATE USER GC_OWNER IDENTIFIED BY "Gcfood@2026"
DEFAULT TABLESPACE TS_MASTER
QUOTA UNLIMITED ON TS_MASTER
QUOTA UNLIMITED ON TS_SALES
QUOTA UNLIMITED ON TS_FINANCE;

GRANT CONNECT, RESOURCE, DBA TO GC_OWNER;


# create table :-- 1. Loại sản phẩm
CREATE TABLE PRODUCT_TYPE (
    product_type_id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    name VARCHAR2(100) NOT NULL,
    description VARCHAR2(500),
    is_active NUMBER(1) DEFAULT 1
) TABLESPACE TS_MASTER;

-- 2. Sản phẩm
CREATE TABLE PRODUCT (
    product_id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    product_type_id NUMBER REFERENCES PRODUCT_TYPE(product_type_id),
    name VARCHAR2(255) NOT NULL,
    description CLOB,
    price NUMBER(18,2),
    application VARCHAR2(255),
    brix NUMBER(5,2),
    size_val VARCHAR2(50), -- đổi tên từ 'size' vì trùng từ khóa
    solid NUMBER(5,2),
    ph NUMBER(4,2),
    is_active NUMBER(1) DEFAULT 1,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP
) TABLESPACE TS_MASTER;

-- 3. Quy cách đóng gói
CREATE TABLE PACKAGING_SPECIFICATION (
    packaging_id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    name VARCHAR2(100),
    unit_of_measure VARCHAR2(50),
    volume NUMBER(10,2),
    material VARCHAR2(100),
    description VARCHAR2(500),
    is_active NUMBER(1) DEFAULT 1
) TABLESPACE TS_MASTER;

-- 4. Chứng chỉ
CREATE TABLE CERTIFICATION (
    certification_id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    name VARCHAR2(255),
    issue_date DATE,
    expiry_date DATE,
    issued_by VARCHAR2(255),
    status VARCHAR2(50),
    description VARCHAR2(500)
) TABLESPACE TS_MASTER;

-- 5. Đảm bảo chất lượng
CREATE TABLE QUALITY_ASSURANCE (
    quality_assurance_id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    name VARCHAR2(255),
    description VARCHAR2(500),
    standard_reference VARCHAR2(255),
    status VARCHAR2(50)
) TABLESPACE TS_MASTER;

-- 6. Chi tiết thông số sản phẩm (Bảng liên kết)
CREATE TABLE PRODUCT_SPECIFICATION (
    product_specification_id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    product_id NUMBER REFERENCES PRODUCT(product_id),
    packaging_id NUMBER REFERENCES PACKAGING_SPECIFICATION(packaging_id),
    certification_id NUMBER REFERENCES CERTIFICATION(certification_id),
    quality_assurance_id NUMBER REFERENCES QUALITY_ASSURANCE(quality_assurance_id),
    shelf_life_days NUMBER,
    storage_condition VARCHAR2(255),
    origin VARCHAR2(100),
    status VARCHAR2(50)
) TABLESPACE TS_MASTER;

-- 7. Loại khách hàng
CREATE TABLE CUSTOMER_TYPE (
    customer_type_id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    name VARCHAR2(100),
    description VARCHAR2(500),
    status VARCHAR2(20)
) TABLESPACE TS_MASTER;

-- 8. Khách hàng
CREATE TABLE CUSTOMER (
    customer_id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    customer_type_id NUMBER REFERENCES CUSTOMER_TYPE(customer_type_id),
    customer_code VARCHAR2(50) UNIQUE,
    company_name VARCHAR2(255) NOT NULL,
    short_name VARCHAR2(100),
    tax_id VARCHAR2(50),
    country VARCHAR2(100),
    city VARCHAR2(100),
    address VARCHAR2(500),
    phone VARCHAR2(20),
    email VARCHAR2(100),
    website VARCHAR2(100),
    industry VARCHAR2(100),
    status VARCHAR2(20),
    preferred_currency VARCHAR2(10),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
) TABLESPACE TS_MASTER;

-- Các bảng danh mục khác
CREATE TABLE STAFF (
    staff_id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    name VARCHAR2(255),
    role VARCHAR2(100),
    email VARCHAR2(100),
    phone VARCHAR2(20),
    department VARCHAR2(100),
    status VARCHAR2(20)
) TABLESPACE TS_MASTER;

CREATE TABLE INCOTERM (
    incoterm_id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    name VARCHAR2(50),
    description VARCHAR2(500),
    version_val VARCHAR2(20),
    status VARCHAR2(20)
) TABLESPACE TS_MASTER;

CREATE TABLE PAYMENT_TERM (
    payment_term_id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    description VARCHAR2(500),
    number_of_days NUMBER,
    status VARCHAR2(20)
) TABLESPACE TS_MASTER;





# Create table for Sale ts :
-- 1. Báo giá
CREATE TABLE QUOTATION (
    quotation_id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    customer_id NUMBER REFERENCES CUSTOMER(customer_id),
    staff_id NUMBER REFERENCES STAFF(staff_id),
    quotation_date DATE,
    expiry_date DATE,
    incoterm_id NUMBER REFERENCES INCOTERM(incoterm_id),
    payment_term_id NUMBER REFERENCES PAYMENT_TERM(payment_term_id),
    total_amount NUMBER(18,2),
    currency VARCHAR2(10),
    status VARCHAR2(20),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
) TABLESPACE TS_SALES;

-- 2. Chi tiết báo giá
CREATE TABLE QUOTATION_ITEM (
    quotation_id NUMBER REFERENCES QUOTATION(quotation_id),
    product_id NUMBER REFERENCES PRODUCT(product_id),
    quantity NUMBER(12,2),
    unit_price NUMBER(18,2),
    discount NUMBER(5,2),
    tax_rate NUMBER(5,2),
    total_price NUMBER(18,2),
    PRIMARY KEY (quotation_id, product_id)
) TABLESPACE TS_SALES;

-- 3. Hợp đồng
CREATE TABLE CONTRACT (
    contract_id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    customer_id NUMBER REFERENCES CUSTOMER(customer_id),
    staff_id NUMBER REFERENCES STAFF(staff_id),
    incoterm_id NUMBER REFERENCES INCOTERM(incoterm_id),
    payment_term_id NUMBER REFERENCES PAYMENT_TERM(payment_term_id),
    contract_date DATE,
    effective_date DATE,
    expiry_date DATE,
    total_contract_value NUMBER(18,2),
    status VARCHAR2(20),
    signed_date DATE
) TABLESPACE TS_SALES;

-- 4. Vận chuyển & Tuyến đường
CREATE TABLE TRANSPORT_ROUTE (
    route_id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    route_name VARCHAR2(255),
    origin_location VARCHAR2(255),
    destination_location VARCHAR2(255),
    distance_km NUMBER(10,2),
    status VARCHAR2(20)
) TABLESPACE TS_SALES;

CREATE TABLE SHIPMENT (
    shipment_id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    route_id NUMBER REFERENCES TRANSPORT_ROUTE(route_id),
    departure_time TIMESTAMP,
    estimated_arrival_time TIMESTAMP,
    status VARCHAR2(20)
) TABLESPACE TS_SALES;


# Finnace :
-- 1. Ngân hàng
CREATE TABLE BANK (
    bank_id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    bank_name VARCHAR2(255),
    account_name VARCHAR2(255),
    account_number VARCHAR2(50),
    swift_code VARCHAR2(20),
    status VARCHAR2(20)
) TABLESPACE TS_FINANCE;

-- 2. Hóa đơn thương mại
CREATE TABLE COMMERCIAL_INVOICE (
    invoice_id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    invoice_date DATE,
    currency VARCHAR2(10),
    total_amount NUMBER(18,2),
    tax_amount NUMBER(18,2),
    due_date DATE,
    status VARCHAR2(20)
) TABLESPACE TS_FINANCE;

-- 3. Thanh toán
CREATE TABLE PAYMENT (
    payment_id NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    expected_amount NUMBER(18,2),
    paid_amount NUMBER(18,2),
    due_date DATE,
    payment_date DATE,
    status VARCHAR2(20)
) TABLESPACE TS_FINANCE;