
CONNECT / AS SYSDBA;


ALTER SESSION SET CONTAINER = FARM_SITE_NAM;
ALTER SESSION SET CURRENT_SCHEMA = FARM_ADMIN;

BEGIN
  FOR t IN (
    SELECT table_name
    FROM all_tables
    WHERE owner = 'FARM_ADMIN'
      AND table_name IN (
        'FARM','WAREHOUSE','SUPPLIER','INGREDIENT','PRODUCT','FARM_PLOT','FARM_BED','IOT_SENSOR','SENSOR_DATA','CULTIVATION_LOG',
        'PURCHASE_CONTRACT','PURCHASE_BATCH','PURCHASE_TICKET','WEIGHING_TICKET','PRODUCTION_LOT','QC_RECORD','TRANSPORT_TICKET','INVENTORY'
      )
  ) LOOP
    BEGIN
      EXECUTE IMMEDIATE 'DROP MATERIALIZED VIEW LOG ON ' || t.table_name;
    EXCEPTION WHEN OTHERS THEN NULL; END;
    EXECUTE IMMEDIATE 'CREATE MATERIALIZED VIEW LOG ON ' || t.table_name || ' WITH PRIMARY KEY, ROWID INCLUDING NEW VALUES';
  END LOOP;
END;
/


ALTER SESSION SET CONTAINER = FARM_SITE;
GRANT CREATE DATABASE LINK TO farm_admin;


CONNECT farm_admin/Aa123@localhost:1521/FARM_SITE;

BEGIN
  FOR m IN (
    SELECT mv_name FROM (
      SELECT 'MV_FARM_NAM' AS mv_name FROM dual UNION ALL
      SELECT 'MV_WAREHOUSE_NAM' FROM dual UNION ALL
      SELECT 'MV_SUPPLIER_NAM' FROM dual UNION ALL
      SELECT 'MV_INGREDIENT_NAM' FROM dual UNION ALL
      SELECT 'MV_PRODUCT_NAM' FROM dual UNION ALL
      SELECT 'MV_FARM_PLOT_NAM' FROM dual UNION ALL
      SELECT 'MV_FARM_BED_NAM' FROM dual UNION ALL
      SELECT 'MV_IOT_SENSOR_NAM' FROM dual UNION ALL
      SELECT 'MV_SENSOR_DATA_NAM' FROM dual UNION ALL
      SELECT 'MV_CULTIVATION_LOG_NAM' FROM dual UNION ALL
      SELECT 'MV_PURCHASE_CONTRACT_NAM' FROM dual UNION ALL
      SELECT 'MV_PURCHASE_BATCH_NAM' FROM dual UNION ALL
      SELECT 'MV_PURCHASE_TICKET_NAM' FROM dual UNION ALL
      SELECT 'MV_WEIGHING_TICKET_NAM' FROM dual UNION ALL
      SELECT 'MV_PRODUCTION_LOT_NAM' FROM dual UNION ALL
      SELECT 'MV_QC_RECORD_NAM' FROM dual UNION ALL
      SELECT 'MV_TRANSPORT_TICKET_NAM' FROM dual UNION ALL
      SELECT 'MV_INVENTORY_NAM' FROM dual
    )
  ) LOOP
    BEGIN
      EXECUTE IMMEDIATE 'DROP MATERIALIZED VIEW ' || m.mv_name;
    EXCEPTION WHEN OTHERS THEN NULL; END;
  END LOOP;
END;
/


CREATE MATERIALIZED VIEW mv_farm_nam REFRESH FAST ON DEMAND AS SELECT * FROM farm@dblink_nam;
CREATE MATERIALIZED VIEW mv_warehouse_nam REFRESH FAST ON DEMAND AS SELECT * FROM warehouse@dblink_nam;
CREATE MATERIALIZED VIEW mv_supplier_nam REFRESH FAST ON DEMAND AS SELECT * FROM supplier@dblink_nam;
CREATE MATERIALIZED VIEW mv_ingredient_nam REFRESH FAST ON DEMAND AS SELECT * FROM ingredient@dblink_nam;
CREATE MATERIALIZED VIEW mv_product_nam REFRESH FAST ON DEMAND AS SELECT * FROM product@dblink_nam;
CREATE MATERIALIZED VIEW mv_farm_plot_nam REFRESH FAST ON DEMAND AS SELECT * FROM farm_plot@dblink_nam;
CREATE MATERIALIZED VIEW mv_farm_bed_nam REFRESH FAST ON DEMAND AS SELECT * FROM farm_bed@dblink_nam;
CREATE MATERIALIZED VIEW mv_iot_sensor_nam REFRESH FAST ON DEMAND AS SELECT * FROM iot_sensor@dblink_nam;
CREATE MATERIALIZED VIEW mv_sensor_data_nam REFRESH FAST ON DEMAND AS SELECT * FROM sensor_data@dblink_nam;
CREATE MATERIALIZED VIEW mv_cultivation_log_nam REFRESH FAST ON DEMAND AS SELECT * FROM cultivation_log@dblink_nam;
CREATE MATERIALIZED VIEW mv_purchase_contract_nam REFRESH FAST ON DEMAND AS SELECT * FROM purchase_contract@dblink_nam;
CREATE MATERIALIZED VIEW mv_purchase_batch_nam REFRESH FAST ON DEMAND AS SELECT * FROM purchase_batch@dblink_nam;
CREATE MATERIALIZED VIEW mv_purchase_ticket_nam REFRESH FAST ON DEMAND AS SELECT * FROM purchase_ticket@dblink_nam;
CREATE MATERIALIZED VIEW mv_weighing_ticket_nam REFRESH FAST ON DEMAND AS SELECT * FROM weighing_ticket@dblink_nam;
CREATE MATERIALIZED VIEW mv_production_lot_nam REFRESH FAST ON DEMAND AS SELECT * FROM production_lot@dblink_nam;
CREATE MATERIALIZED VIEW mv_qc_record_nam REFRESH FAST ON DEMAND AS SELECT * FROM qc_record@dblink_nam;
CREATE MATERIALIZED VIEW mv_transport_ticket_nam REFRESH FAST ON DEMAND AS SELECT * FROM transport_ticket@dblink_nam;
CREATE MATERIALIZED VIEW mv_inventory_nam REFRESH FAST ON DEMAND AS SELECT * FROM inventory@dblink_nam;

BEGIN
  DBMS_SCHEDULER.drop_job('REFRESH_MV_NAM', force => TRUE);
EXCEPTION WHEN OTHERS THEN NULL; END;
/
BEGIN
  DBMS_SCHEDULER.create_job(
    job_name        => 'REFRESH_MV_NAM',
    job_type        => 'PLSQL_BLOCK',
    job_action      => 'BEGIN DBMS_MVIEW.REFRESH(''MV_FARM_NAM,MV_WAREHOUSE_NAM,MV_SUPPLIER_NAM,MV_INGREDIENT_NAM,MV_PRODUCT_NAM,MV_FARM_PLOT_NAM,MV_FARM_BED_NAM,MV_IOT_SENSOR_NAM,MV_SENSOR_DATA_NAM,MV_CULTIVATION_LOG_NAM,MV_PURCHASE_CONTRACT_NAM,MV_PURCHASE_BATCH_NAM,MV_PURCHASE_TICKET_NAM,MV_WEIGHING_TICKET_NAM,MV_PRODUCTION_LOT_NAM,MV_QC_RECORD_NAM,MV_TRANSPORT_TICKET_NAM,MV_INVENTORY_NAM'',''F''); END;',
    start_date      => SYSTIMESTAMP,
    repeat_interval => 'FREQ=MINUTELY;INTERVAL=5',
    enabled         => TRUE
  );
END;
/


BEGIN
  DBMS_MVIEW.REFRESH('MV_FARM_NAM,MV_WAREHOUSE_NAM,MV_SUPPLIER_NAM,MV_INGREDIENT_NAM,MV_PRODUCT_NAM,MV_FARM_PLOT_NAM,MV_FARM_BED_NAM,MV_IOT_SENSOR_NAM,MV_SENSOR_DATA_NAM,MV_CULTIVATION_LOG_NAM,MV_PURCHASE_CONTRACT_NAM,MV_PURCHASE_BATCH_NAM,MV_PURCHASE_TICKET_NAM,MV_WEIGHING_TICKET_NAM,MV_PRODUCTION_LOT_NAM,MV_QC_RECORD_NAM,MV_TRANSPORT_TICKET_NAM,MV_INVENTORY_NAM','F');
END;
/
