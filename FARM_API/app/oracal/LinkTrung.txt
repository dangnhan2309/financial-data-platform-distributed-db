
CONNECT / AS SYSDBA;


ALTER SESSION SET CONTAINER = FARM_SITE_TRUNG;
ALTER SESSION SET CURRENT_SCHEMA = FARM_ADMIN;

BEGIN
  FOR t IN (
    SELECT table_name
    FROM all_tables
    WHERE owner = 'FARM_ADMIN'
      AND table_name IN (
        'FARM','WAREHOUSE','SUPPLIER','INGREDIENT','PRODUCT','FARM_PLOT','FARM_BED','IOT_SENSOR','SENSOR_DATA','CULTIVATION_LOG',
        'PURCHASE_CONTRACT','PURCHASE_BATCH','PURCHASE_TICKET','WEIGHING_TICKET','PRODUCTION_LOT','QC_RECORD','TRANSPORT_TICKET','INVENTORY'
      )
  ) LOOP
    BEGIN
      EXECUTE IMMEDIATE 'DROP MATERIALIZED VIEW LOG ON ' || t.table_name;
    EXCEPTION WHEN OTHERS THEN NULL; END;
    EXECUTE IMMEDIATE 'CREATE MATERIALIZED VIEW LOG ON ' || t.table_name || ' WITH PRIMARY KEY, ROWID INCLUDING NEW VALUES';
  END LOOP;
END;
/


ALTER SESSION SET CONTAINER = FARM_SITE;
GRANT CREATE DATABASE LINK TO farm_admin;


CONNECT farm_admin/Aa123@localhost:1521/FARM_SITE;

BEGIN EXECUTE IMMEDIATE 'DROP DATABASE LINK dblink_trung'; EXCEPTION WHEN OTHERS THEN NULL; END;
/
CREATE DATABASE LINK dblink_trung CONNECT TO farm_admin IDENTIFIED BY "Aa123"
  USING '(DESCRIPTION=(ADDRESS=(PROTOCOL=TCP)(HOST=localhost)(PORT=1521))(CONNECT_DATA=(SERVICE_NAME=FARM_SITE_TRUNG)))';


BEGIN
  FOR m IN (
    SELECT mv_name FROM (
      SELECT 'MV_FARM_TRUNG' AS mv_name FROM dual UNION ALL
      SELECT 'MV_WAREHOUSE_TRUNG' FROM dual UNION ALL
      SELECT 'MV_SUPPLIER_TRUNG' FROM dual UNION ALL
      SELECT 'MV_INGREDIENT_TRUNG' FROM dual UNION ALL
      SELECT 'MV_PRODUCT_TRUNG' FROM dual UNION ALL
      SELECT 'MV_FARM_PLOT_TRUNG' FROM dual UNION ALL
      SELECT 'MV_FARM_BED_TRUNG' FROM dual UNION ALL
      SELECT 'MV_IOT_SENSOR_TRUNG' FROM dual UNION ALL
      SELECT 'MV_SENSOR_DATA_TRUNG' FROM dual UNION ALL
      SELECT 'MV_CULTIVATION_LOG_TRUNG' FROM dual UNION ALL
      SELECT 'MV_PURCHASE_CONTRACT_TRUNG' FROM dual UNION ALL
      SELECT 'MV_PURCHASE_BATCH_TRUNG' FROM dual UNION ALL
      SELECT 'MV_PURCHASE_TICKET_TRUNG' FROM dual UNION ALL
      SELECT 'MV_WEIGHING_TICKET_TRUNG' FROM dual UNION ALL
      SELECT 'MV_PRODUCTION_LOT_TRUNG' FROM dual UNION ALL
      SELECT 'MV_QC_RECORD_TRUNG' FROM dual UNION ALL
      SELECT 'MV_TRANSPORT_TICKET_TRUNG' FROM dual UNION ALL
      SELECT 'MV_INVENTORY_TRUNG' FROM dual
    )
  ) LOOP
    BEGIN
      EXECUTE IMMEDIATE 'DROP MATERIALIZED VIEW ' || m.mv_name;
    EXCEPTION WHEN OTHERS THEN NULL; END;
  END LOOP;
END;
/


CREATE MATERIALIZED VIEW mv_farm_trung REFRESH FAST ON DEMAND AS SELECT * FROM farm@dblink_trung;
CREATE MATERIALIZED VIEW mv_warehouse_trung REFRESH FAST ON DEMAND AS SELECT * FROM warehouse@dblink_trung;
CREATE MATERIALIZED VIEW mv_supplier_trung REFRESH FAST ON DEMAND AS SELECT * FROM supplier@dblink_trung;
CREATE MATERIALIZED VIEW mv_ingredient_trung REFRESH FAST ON DEMAND AS SELECT * FROM ingredient@dblink_trung;
CREATE MATERIALIZED VIEW mv_product_trung REFRESH FAST ON DEMAND AS SELECT * FROM product@dblink_trung;
CREATE MATERIALIZED VIEW mv_farm_plot_trung REFRESH FAST ON DEMAND AS SELECT * FROM farm_plot@dblink_trung;
CREATE MATERIALIZED VIEW mv_farm_bed_trung REFRESH FAST ON DEMAND AS SELECT * FROM farm_bed@dblink_trung;
CREATE MATERIALIZED VIEW mv_iot_sensor_trung REFRESH FAST ON DEMAND AS SELECT * FROM iot_sensor@dblink_trung;
CREATE MATERIALIZED VIEW mv_sensor_data_trung REFRESH FAST ON DEMAND AS SELECT * FROM sensor_data@dblink_trung;
CREATE MATERIALIZED VIEW mv_cultivation_log_trung REFRESH FAST ON DEMAND AS SELECT * FROM cultivation_log@dblink_trung;
CREATE MATERIALIZED VIEW mv_purchase_contract_trung REFRESH FAST ON DEMAND AS SELECT * FROM purchase_contract@dblink_trung;
CREATE MATERIALIZED VIEW mv_purchase_batch_trung REFRESH FAST ON DEMAND AS SELECT * FROM purchase_batch@dblink_trung;
CREATE MATERIALIZED VIEW mv_purchase_ticket_trung REFRESH FAST ON DEMAND AS SELECT * FROM purchase_ticket@dblink_trung;
CREATE MATERIALIZED VIEW mv_weighing_ticket_trung REFRESH FAST ON DEMAND AS SELECT * FROM weighing_ticket@dblink_trung;
CREATE MATERIALIZED VIEW mv_production_lot_trung REFRESH FAST ON DEMAND AS SELECT * FROM production_lot@dblink_trung;
CREATE MATERIALIZED VIEW mv_qc_record_trung REFRESH FAST ON DEMAND AS SELECT * FROM qc_record@dblink_trung;
CREATE MATERIALIZED VIEW mv_transport_ticket_trung REFRESH FAST ON DEMAND AS SELECT * FROM transport_ticket@dblink_trung;
CREATE MATERIALIZED VIEW mv_inventory_trung REFRESH FAST ON DEMAND AS SELECT * FROM inventory@dblink_trung;


BEGIN
  DBMS_SCHEDULER.drop_job('REFRESH_MV_TRUNG', force => TRUE);
EXCEPTION WHEN OTHERS THEN NULL; END;
/
BEGIN
  DBMS_SCHEDULER.create_job(
    job_name        => 'REFRESH_MV_TRUNG',
    job_type        => 'PLSQL_BLOCK',
    job_action      => 'BEGIN DBMS_MVIEW.REFRESH(''MV_FARM_TRUNG,MV_WAREHOUSE_TRUNG,MV_SUPPLIER_TRUNG,MV_INGREDIENT_TRUNG,MV_PRODUCT_TRUNG,MV_FARM_PLOT_TRUNG,MV_FARM_BED_TRUNG,MV_IOT_SENSOR_TRUNG,MV_SENSOR_DATA_TRUNG,MV_CULTIVATION_LOG_TRUNG,MV_PURCHASE_CONTRACT_TRUNG,MV_PURCHASE_BATCH_TRUNG,MV_PURCHASE_TICKET_TRUNG,MV_WEIGHING_TICKET_TRUNG,MV_PRODUCTION_LOT_TRUNG,MV_QC_RECORD_TRUNG,MV_TRANSPORT_TICKET_TRUNG,MV_INVENTORY_TRUNG'',''F''); END;',
    start_date      => SYSTIMESTAMP,
    repeat_interval => 'FREQ=MINUTELY;INTERVAL=5',
    enabled         => TRUE
  );
END;
/


BEGIN
  DBMS_MVIEW.REFRESH('MV_FARM_TRUNG,MV_WAREHOUSE_TRUNG,MV_SUPPLIER_TRUNG,MV_INGREDIENT_TRUNG,MV_PRODUCT_TRUNG,MV_FARM_PLOT_TRUNG,MV_FARM_BED_TRUNG,MV_IOT_SENSOR_TRUNG,MV_SENSOR_DATA_TRUNG,MV_CULTIVATION_LOG_TRUNG,MV_PURCHASE_CONTRACT_TRUNG,MV_PURCHASE_BATCH_TRUNG,MV_PURCHASE_TICKET_TRUNG,MV_WEIGHING_TICKET_TRUNG,MV_PRODUCTION_LOT_TRUNG,MV_QC_RECORD_TRUNG,MV_TRANSPORT_TICKET_TRUNG,MV_INVENTORY_TRUNG','F');
END;
/
